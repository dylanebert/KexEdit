[package]
name = "kexedit-core"
authors = ["IndividualKex"]
license = "MIT"
version = "0.1.0"
edition = "2021"
description = "Force Vector Design physics simulation for roller coaster track generation"

[lib]
crate-type = ["cdylib", "rlib"]
name = "kexengine"

[features]
default = ["ffi"]
ffi = []

[dependencies]
approx = "0.5"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
strip = true
