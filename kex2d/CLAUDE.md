# kex2d

2D coaster solver prototype. Shallot + Svelte + canvas2D. The exploration harness for a free-drag track model (free node positions → cubic Hermite interpolation → physical force recovery → F_n curve), mouse-driven and direct, parallel to `app/` (the eventual Shallot port). Whether it replaces / augments / coexists with the 3D editor decides once it earns its place.

Next steps live in `kex/roadmap.md` "kex2d"; in-flight design rationale + the unbuilt optimizer design in `kex/scratch.md` "kex2d solver".

## Model

Free-drag authoring, mouse-driven. The **control scheme** and the **representation** are separate: the controls place free node positions, dragged directly; the canonical representation is the F_n curve. Each node carries a position (dragged) **and a stored heading θ**, never directly authored — derived from position by the circular-arc reflection (below). The **first node is a fixed flat anchor** (θ = 0): the track always launches horizontally.

- **Interpolate.** `sampleChain` (`spline.ts`) samples a cubic Hermite curve through every node, the tangent **direction** read from each node's stored heading and the tangent **length** scaled by the live chord (`|T| = chord·sec²(φ/2)`, the cubic best-fit to a circular arc — `k=1` for straight, saturated for near-U-turns). Strict local support: a drag moves **only the two segments that share the dragged node** — nothing before the previous node or after the next moves. The **last** (heading) node carries a standing invariant — its angle is always the reflection of its predecessor's heading — so its segment stays a clean arc and its angle never goes stale and jumps. It re-derives (`headLast`) whenever the tail changes: a drag of the tip or the node before it, or a deletion that promotes a new tip. The **first** node is a fixed flat anchor and **interior** nodes keep their heading frozen: the arc contract can't hold on both of an interior node's segments at once, so a stable heading is chosen over one that thrashes (lengths still re-proportion to the live chord). A frozen interior heading dragged far off its chord bulges — the accepted misshaping.
- **Recover force.** `forces` (`bake.ts`) reads the sampled positions → per-sample tangent θ (the curve's local tangent, bisector of adjacent chords) → v (energy) → `F_n = κ·v²/g + cos θ`, the physical normal force a cart riding the curve feels. This per-sample θ is recovered from the geometry, distinct from the node headings that shape the curve. Deliberately *not* the algebraic inverse `invertRange` (that sawtooths — see "Hard gotchas"). A Hermite cubic's curvature varies within a span, so F_n is smoothly varying — the target shape for the optimizer, not a regression.

F_n smoothing beyond this is the optimizer's job (`solve.ts` / `optimize.ts`, roadmap "kex2d Phase 1"), so the bake stays geometry-only and simple.

Lossy bake (Houdini/Blender modifier-stack analogue): parametric authoring is one-shot, canonical state lives in the dense baked form, re-authoring overwrites whatever F_n was there. After the bake, F_n is canonical.

## Physics — forward integrator + force recovery

Per-sample state `(x, y, θ, v)`. Semi-implicit Euler in arclength, source-σ convention (F_n sampled at `σ_i = i·Δs` drives step i → i+1):

```
dθ       = (F_n(σ_i) − cos θ_i) · g · Δs / v_i²
θ_{i+1}  = θ_i + dθ
midθ     = ½(θ_i + θ_{i+1})
x_{i+1}  = x_i + Δs · cos(midθ)
y_{i+1}  = y_i + Δs · sin(midθ)
v_{i+1}² = v_i² − 2g · (y_{i+1} − y_i)
```

Velocity uses the energy-delta (squared) form to avoid catastrophic cancellation. Clamps: `vSafe = max(|v|, V_FLOOR)` in the dθ formula, `v_next = sqrt(max(v_next², 0))`. Both are non-differentiable — the optimizer's gradient sees kinks at the boundary, so the loss should keep the chain out of that regime.

**Force recovery** (`bake.ts forces`, the bake path): positions → per-sample tangent θ → v → F_n.

- `m_i = atan2(y_{i+1} − y_i, x_{i+1} − x_i)` — edge (chord) angle, accumulated *continuously* (unwrapped) so θ stays continuous across the ±π branch cut (the cart lerps θ for its orientation)
- `θ_i = ½(m_{i−1} + m_i)` — the curve's local tangent (bisector of adjacent chords); free ends extrapolate the bisector trend (exact for constant curvature, second-order otherwise)
- `v_i² = v_0² − 2g·(y_i − y_0)` — energy conservation; `v_i = sqrt(max(0, v_i²))` to match the forward clamp
- `F_n[i] = (θ_{i+1} − θ_i)·vSafe_i² / (g·Δs) + cos(θ_i)` = κ·v²/g + cos θ

The other recovery, `invertRange` (`θ_{i+1} = 2·m_i − θ_i`), is the integrator's exact reflection inverse — round-trip + optimizer only, not the bake (Hard gotchas).

Constants: `V_FLOOR` = 0.01 (numerical floor, ½v² ≈ 5e-5 J/kg) in `forward.ts` (the integrator owns the `vSafe` clamp; `bake.ts` re-exports it); `V_WARN` = 1.0 (diagnostic threshold for the red-track / red-handle / warning-banner UX, distinct from the floor) in `bake.ts`; `MAX_U_PER_EDGE` = π/24 (angular-cap floor on per-edge turning, keeps tight arcs sample-dense) in `spline.ts`; `MAX_SAMPLES` = 4096 in `track.ts`.

## Code map

- `forward.ts` — `step` + `integrate`. Four SoA Float32Arrays + `count`, `ds`, `fN(σ)`; index 0 pre-set, writes `1..count−1`. The forward model the optimizer rides solved F_n through (`replay`→`step`); not on the bake path.
- `spline.ts` — Hermite interpolation (pure, no shallot import). `sampleChain` (nodes with stored headings → positions + per-edge chord `dsArr` + node sample `offsets`; tangent length scaled by the live chord, edge count = arc-length spacing OR the angular-cap floor, whichever demands more). `reflect` (the circular-arc exit heading `2·chord − prev` — track.ts's authoring primitive), `MAX_U_PER_EDGE`. No neighbor-derived tangents, no `ALPHA` — headings are stored state, not inferred from positions.
- `bake.ts` — `forces` (the bake path: positions → smooth tangent θ → v → physical `F_n = κ·v²/g + cos θ`, per-edge ds). `invertRange`/`invert` (the integrator's *exact* reflection inverse, for round-trip validation, NOT the bake — see "Hard gotchas"), `replay` (forward-integrate F_n back to positions — round-trip tests + `optimize.ts`'s realized ride), `resampleByTime` (per-edge F_n → uniform draft-time grid, linearly interpolated — continuous so a node nudge doesn't flicker the prior; timeline dots + optimizer prior share it). `V_WARN` + re-exports `forward`'s `V_FLOOR`.
- `solve.ts` — the convex F_n optimizer (pure, no ECS). `solve(Fpos, opts)` minimizes data L2 + second-difference smoothness + a soft force-limit band hinge over the solved F_n; a banded pentadiagonal LDLᵀ (`pentaFactor` + `pentaSolveFactored`) wrapped in an active-set loop for the hinge (warm-startable). `DEFAULT_BAND` `[−2, 6]g`, `DEFAULT_SMOOTH`/`DEFAULT_BAND_WEIGHT` (live feel weights, tune by eye), `DEFAULT_PIN_WEIGHT`/`DEFAULT_ANCHOR_WEIGHT` (Phase 2b, both `1e4`). `con` (point force pins — constant diagonal data terms) and `anchors` (recovery rows from `anchor.ts` — dense, entering via a **Woodbury** low-rank update around the band; the bias `U W t`, `t=row·Fpos`, pulls geometry to the *draft*, not 0; the active-set test reads the post-Woodbury solution); both wired into the live solve by `optimize.ts` (Phase 2b). Roadmap "kex2d Phase 1–2".
- `collocate.ts` — the **position-space collocation solver** (Phase 3, pure, no ECS). `collocate(draftX, draftY, n, v0, opts)` optimizes the sample *geometry* (not F_n): residuals = shape-identity + soft band-hinge + `ForceTarget` pins + geometric jerk + linear height bound + flat-launch, solved by a banded-Cholesky Gauss-Newton + backtracking line search; the force is read off the geometry (`forces64`, the f64 mirror of `bake.forces`; `forceLocal` its per-edge local form). Supersedes `solve.ts`/`anchor.ts` but **not yet wired** — `optimize.ts` still runs the F_n `solve()`. Wants ds ≈ 0.5 (1/ds² stiffness). Wiring + retirement of the F_n path: roadmap "kex2d Phase 3".
- `bezier.ts` — the **bezier constraint draft** (Phase 2c, pure). `bezierY` (a unit-box cubic with x control points in [0,1] inverted for s via Newton + bisection fallback — the WebKit `UnitBezier::solve` shape — then y(s) read off arbitrary g-space control points) and `constraintTargets(pins, N)` (the piecewise cubic through the index-sorted pins, evaluated on the draft grid → one `{index, value}` per grid index in the span; 1 pin → a lone point, 0 → none; same-index pins collapse highest-id-wins). The y axis is **absolute g** (a handle `dy` is a g-delta from the keyframe value), NOT theatre's value-delta normalization — so a flat segment still bumps/overshoots (Hard gotchas).
- `anchor.ts` — `recoveryRows(theta, v, ds, r)`: the recovery-anchor sensitivity rows (pure). The exact tangent (variational) linearization of the forward rollout about the draft — the `(θ, s=v²)` subsystem closes without `x,y`, propagated forward (O(N²)) to the three dense rows `∂x_r/∂F`, `∂y_r/∂F`, `∂θ_r/∂F` (nonzero only for `i<r` by causality). NOT the frozen-velocity sketch — it keeps the heading + velocity feedbacks, so it matches the true FD Jacobian. The anchor pulls these geometry quantities at a recovery point `r` back to the draft, healing the downstream swing a force pin causes. Roadmap "kex2d Phase 2a".
- `track.ts` — `BakeSystem` gathers node positions + headings from sorted handles, calls `sampleChain` then `forces`, syncs each node's `Handle.sample`. `Handle` carries `order`, `sample`, `pos` (the free authored position; the curve passes through it exactly, never written back), and `theta` (the exit heading — see Model). `bakeOut` carries per-edge `fN`+`ds`, per-sample cumulative `t`, `feasible: Uint8Array`, `firstInfeasible`, `lastBakedOrder` (orphaned nodes render red), `hash` (input-state gate = every node's pos + theta; a miss triggers a re-bake). `createTrack` (buffers) / `addNode(x, y)` (append) / `extend` (lay a node along the last heading by `EXTEND_DIST`) / `reheadOnDrag(eid)` + `headLast` (post-drag heading refresh) / `removeTrailingHandle` (drop the last node) / `sortedHandles` / `lastHandle`. Undo/redo primitives (composed by `history.ts`, mirroring `pins.ts`'s snapshot ops): `handleAt(order)` (order→eid), `spawnNode(order,x,y,theta)` (re-create a node verbatim — no `reflect`/rehead), `nodeSnapshot`/`restoreNodes` (whole-chain pose by order, for the move gesture).
- `optimize.ts` — `SolveSystem` (runs after `BakeSystem`; gate = bake `hash` + pin `rev`) resamples the baked F_n onto the uniform draft-time grid, builds `con` from the bezier constraint draft (`constraintTargets` over the authored pins — Phase 2c; one pin stays a single point term; no pins → `[]`) plus **one always-on endpoint recovery anchor** (replay the draft prior over `dsGrid` → `recoveryRows(θ°,v°,dsGrid,N−1)` → x/y/θ rows, three DOF at the endpoint fix the rigid downstream transform), `solve`s, then rides the solved force from the draft's launch over the draft's per-step arclength → `solveOut[trackEid]` (the realized/ridden track: solved `fN` on the draft-time grid + realized `posX/posY/theta/v` + realized cumulative `t` + `firstInfeasible` + `gate`). **One unified path, no pin/no-pin branch:** smoothness, the soft band, and the recovery anchor are structural; pins are just an extra data term. So *any* force-space deviation — the band clamping the draft's own out-of-band forces, smoothness, or a pin — recovers downstream toward the draft instead of diverging (the no-pin band-clamp swing is the screenshot bug `anchor.test.ts` guards). This is what the cart rides — `forward(solved F_n)` — distinct from the dotted position draft. `OPT_GRID = 256`.
- `cart.ts` — looping cart animation on the *realized* (solved) track. `cartState[trackEid]` carries `t` (advances by real `dt`, wrapping at `loopTime(solveOut)` — the realized track's first infeasible time, or its `tTotal`) and `held` (set while the timeline playhead is scrubbed or playback is paused; the advance loop still refreshes `lastClock` but yields `t`, so release doesn't replay the gap). `cartPose(trackEid, t)` interps `solveOut` for the box renderer and returns `u` (draft-time grid-fraction progress, the playhead position); `cartTimeAtU(trackEid, u)` is its inverse (grid-fraction → realized time, what a scrub writes into `t`); `sampleFNOverTime` resamples the *baked* F_n onto a uniform-time grid (the timeline's draft dots).
- `editor.ts` — ephemeral UI state: just `selection`. No tools or modes. Plain singleton, read by Svelte via the per-RAF tick.
- `pins.ts` — authored force pins (Phase 2b/2c), **canonical** per-track authoring data keyed by trackEid (NOT ephemeral; serializes with the track when save/load lands). `Pin {id,index,value,hl,hr}` — `index` a draft-time grid index in `[0,OPT_GRID)`, `value` in g; a stable monotonic `id` (undo addresses pins by id, surviving remove→undo→set where a recycled eid would not); `hl`/`hr` the bezier tangent `Handle {dx,dy}` (Phase 2c — `dx∈[0,1]` outward from the keyframe, `dy` a free g-delta; `defaultHandle` = `{1/3, 0}`). `pinsOf`/`addPin`/`insertPin`/`removePin`/`setPin` (index+value only) /`setHandle` (one tangent, clamps `dx`) /`findPin`/`clearPins` + `pinSnapshot`/`restorePin` (deep-copy the handle objects — gesture/undo state must not alias the live pin) + `pinRev` (monotonic, folded into the solve gate so a pin edit re-solves with no bake change). `createTrack` calls `clearPins` (a fresh track carries none).
- `history.ts` — **one undo/redo stack for the whole editor**, mirroring shallot's editor (`document/index.ts`). The substrate is domain-agnostic: a `Command {apply, reverse}` do/undo pair on a dual stack (`MAX_UNDO=256`), plus a **generic snapshot gesture** — `begin(snap, restore, same)` / `commit` / `cancel` parameterized by closures, one gesture open at a time, so any domain's live drag collapses to a single entry. Two authoring surfaces record onto it, each addressed by a **stable identity** that survives its slot/eid moving under undo: **pins** (`beginPin` + `drop`/`erase`, by pin `id`) and **track nodes** (`extendTrack`/`trimTrack` + `beginMove`, by `Handle.order` — stable because the chain is append/delete-trailing only). `trimTrack` captures the promoted tip's heading before+after (the `headLast` rehead) and restores it on either side; `extendTrack` undo is a plain removal (extend never reheads). Do-paths mutate live (instant re-bake via the bake `hash` / re-solve via `pinRev`) + record an already-applied command; only undo/redo replay through apply/reverse — no ECS-sync layer (both stores are canonical). Selection stays ephemeral, out of the commands. `history` singleton for the app; `createHistory` for tests.
- `controls.ts` — `attachControls(canvas, ecs)` wires canvas pointer + window keyboard and returns a teardown; called from App's `onMount` so listeners live with the canvas (no module-flag staleness). `pickNode` (nearest within `PICK_R`); pointerdown picks + drags a node (or deselects on empty), drag sets `Handle.pos` to the cursor with a grab offset so grabbing off-center doesn't snap then calls `reheadOnDrag` to refresh the last node's heading (first node + interior stay frozen); `Enter` extends, `Del` removes the trailing node when the end is selected. All three edits route through `history` (`beginMove`+`commit` on pointerup / `cancel` on pointercancel; `extendTrack`/`trimTrack` on the keys) onto the shared undo stack.
- `timeline.ts` — pure transform + tick math for the force-curve timeline (no Svelte/DOM/solve state). `View` = `{pan, pxPerSec}`; `secToPx`/`pxToSec` (the second↔pixel affine), `zoomAt` (cursor-anchored geometric zoom — the second under the cursor stays fixed; clamp scale *before* deriving pan), `clampView` (left edge anchored at t=0 — no negative time on the ruler; min-scale fits the whole track + a one-sided right lead-out, left-aligned), `frameAll`, `niceStep` (1-2-5×10ⁿ, geometric-mean breakpoints), `ticks`, plus the time-navigator math: `navWindow` (the visible span as `{l,r}` fractions of the full track + lead-out) and `navDragView` (apply an overview drag — `pan` slides the window, `l`/`r` drag one edge with the opposite anchored, a zoom; span floors at the `MAX_PX_PER_SEC` zoom ceiling). Ported from `reference/animation-timeline`; unit-tested in `tests/timeline.test.ts`.
- `Timeline.svelte` — the always-present bottom dock. Top→bottom bands (`RULER_H`/`GAP_H`/`TOP`/`BOT_PAD`): a **ruler** (adaptive 1-2-5 ticks + labels + playhead handle), a darker **marker lane** (`GAP_H`, a full row — sized for event markers later, Unity-timeline style), then the curve chart (baked F_n dots + solved curve, canvas2D). The ruler is the **scrub zone** — click/drag anywhere positions the playhead and *parks* paused on release (`startScrub`/`endScrub`, never auto-resumes); the chart owns pin authoring, so scrub and pin-drop don't compete. The transport is a separate **media player** floated below the dock at the screen's bottom edge (play/pause + Space · global full-track slider · timecode), its slider the global scrubber to the ruler's zoomed-local playhead. Layout posture + the player-vs-timeline split live in `.claude/rules/editor-ui.md`. The Y axis **auto-fits** the data (`yFit` in `timeline.ts`: scans solved curve + baked dots + pin values + tangent-handle control points, always keeps 1g, rounds OUTWARD to a nice 1-2-5 step; `CAP_LO/CAP_HI` bound the zoom-out + clamp pin values, never the display range). `yTarget` is a **stable default frame** (`FIT_FLOOR`, a calm window around 1g) that only **expands** to fit data beyond it — it never hugs tight, so zero vs one keyframe near 1g shows the *same* axis (no jarring rescale) and the meaningful g-scale stays put. This is the After Effects / Unity curve-view posture: keep the view steady, grow only when content would clip, don't auto-re-tighten. The **displayed** `yView` approaches the target ASYMMETRICALLY — grows fast (`Y_OUT`), contracts lazily (`Y_IN`) — the modern-web "sticky viewport" so an edit grows the axis promptly but releasing never snaps it back. While a pin/handle is dragged the axis holds steady (no jitter from the live re-solve, and grabbing a near-edge keyframe doesn't move it) UNTIL the cursor is dragged PAST the chart edge, where `yGrow` edge-scrolls it to follow (the standard "scroll only when the pointer leaves the viewport" rule — grow ∝ distance *past* the edge, never on an inset pad, per-frame) and the drag re-applies through the grown axis so the element keeps moving while held beyond the edge (a pin caps at `CAP`, a tangent handle follows further to `HANDLE_CAP` since handle overshoot is authorable past the band). One `$effect` paced by `tick` drives all of it, its whole body wrapped in `untrack` (it reads + writes `yView` and re-runs the drag appliers, which read `yView` — a tracked read would self-retrigger to convergence). `yView`'s `step` is derived from the *displayed* span (not the target's) so a lazy contraction doesn't crowd labels. Feel constants `FIT_FLOOR`/`Y_OUT`/`Y_IN`/`EDGE_RATE` are by-eye. g gridlines + labels live in a **left gutter** (`LEFT_GUT`), so the chart insets: the time affine lives in `[LEFT_GUT, w]` and every `timeline.ts` call takes `chartW = w − LEFT_GUT` with `LEFT_GUT` added/subtracted at the content↔screen boundary. Band-limit lines draw only when within the fit range. View-state is one `{pan, pxPerSec}` in `$state`; a `clamped` `$derived` re-fits it to the live width/track-length (so a resize or edit never writes back into `view` — no effect loop). Plain wheel zooms (geometric, cursor-anchored), shift+wheel pans, the curve-editor standard (Unity/AE); a trackpad's horizontal axis pans and a pinch (ctrl+wheel) zooms. Both clamped to the track extent — so zooming fully out *is* frame-all (no separate button). A **time navigator** (`.nav`, below the chart — the Premiere scrollbar placement) is a **preview minimap** (VSCode / DAW-overview style): `renderNav` draws a faint miniature of the full-track solved F_n curve (y-range tracks `yView`; the curve fills only `tTotal/(tTotal+margin)` of the width) into `navCanvas`, and a translucent viewport window over it drags to pan / edge-resizes to zoom (the opposite edge anchored) via `navWindow`/`navDragView`. Its inner track insets by `LEFT_GUT` to align with the chart's time axis; the whole lane dims only when there's no track. The g-axis labels **hide** (not nudge) when a gridline sits within `LABEL_HALF` of the plot's top/bottom edge, so an extreme label never bleeds into the marker lane or below. Opaque surface, **no resize** (the editor posture in the rule above). X-axis = draft-time grid seconds; the cart clock is realized time, bridged by `u` (consistent only because the axis samples at `OPT_GRID`). **Authors force pins + the bezier constraint draft (Phase 2b/2c)** via the Unity/After-Effects curve-editor interaction (no tools, no modes): **double-click** the chart adds a pin (+ selects it) at the clicked time but on the *evaluated curve* (the solved value there, not the cursor height — so the add doesn't jolt the shape; cursor height is only the no-curve-yet fallback), **single-click a pin** selects it (persistent `selectedId` keeps its handles shown), **double-click a pin** (`pinDblClick`) opens the inline value editor to type a precise g (a single clean numeric field — `type=number` spinners hidden, value rounded; delete is right-click/Del so the popup carries no chrome), **drag** = live re-solve / commit-on-release, **right-click a pin or Del** deletes (`deletePin`; the Del branch `stopImmediatePropagation`s so `controls.ts`'s node-trim doesn't also fire), **single-click empty** deselects, **middle-drag** pans (intercepted at the host's `onpointerdowncapture` so it beats the `pointer-events:all` rects). Per-pin fat hit-zone + thin dot (theatre `GraphEditorDotScalar`), `cursor: move` (the graph-editor standard for an x+y point — not `grab`, the canvas-pan hand); all routed through `history` (Ctrl/⌘-Z / -Y / -Shift-Z). `valOf` is `yOf`'s inverse; `indexAt`/`valAt` map screen→pin. With ≥2 pins the **drawn constraint curve** renders as a dashed blue piecewise cubic (canvas `bezierCurveTo` on the screen-mapped control points — `pxOfIndex`/`yOf` are affine, so the screen cubic equals the value-space cubic exactly); **every** pin shows **both** tangent handles always (the After Effects default; inactive pins draw quiet, the `activeId` pin brightens). `spanOf` gives the length basis (gap to the neighbor), an endpoint **borrows its one adjacent segment**, and a **lone pin falls back to `DEFAULT_SPAN`** — handles exist for ANY pin count, no hiding branch (a single point shows its tangent; only the *drawn* curve needs ≥2 pins, a bezier segment needing two endpoints). Handle control points are **first-class for scaling** — `yTarget` scans `pin.value ± h.dy`, so a handle can't settle off-screen. A handle drag X-clamps `dx∈[0,1]` (function-of-time) and **clamps the cursor Y to the chart** so the nub sticks to the edge (edge-grow drives its value past the band) instead of flying off the dock; `dy = valOf(cy) − pin.value`, then `mirrorTangent` sets the OTHER handle collinear-opposite at its preserved length (free lengths, smooth tangent — **auto/continuous mode**), live re-solve / commit-on-release. The drawn curve is the *constraint target* (blue); the cart rides the *solved* curve (orange) the anchor heals downstream.
- `App.svelte` / `render.ts` / `view.ts` — Svelte shell + canvas2D render: grid, the **position draft** polyline (dotted, de-emphasized; feasible blue / infeasible red passes), the **realized track** overlaid solid (`solveOut`, accent, clipped at realized infeasibility), the node handles (selected highlighted, orphan/infeasible red), the cart (on the realized track), the **Timeline** dock (the `Timeline.svelte` component above), and the radial extend/delete buttons (DOM, positioned around the selected chain end via `viewTransform` — extend along the heading, delete rotated off it).

## Editing model

No tools, no modes — just selection. Click a node to select + drag it freely to where you want it; click empty space to deselect. A drag reshapes exactly the two segments sharing the dragged node, no cascade either direction.

- **Free drag** (any node): pointerdown picks the nearest node within `PICK_R` and drags it; `Handle.pos` follows the cursor with a grab offset so grabbing off-center doesn't snap, then `reheadOnDrag` refreshes the heading per the Model.
- **Extend / Delete** (radial buttons around the selected chain end): Extend (＋, placed along the heading) lays a node continuing the last edge's direction by `EXTEND_DIST` + selects it (`extend`, also `Enter`); Delete (🗑, rotated off extend) removes the trailing node + selects the new end (`removeTrailingHandle`, also `Del`), re-heading the promoted tip so it doesn't jump when grabbed, never below the two nodes a chain needs.
- **No insert-on-curve, no interior insertion.** Append/drag/delete only — interior insertion is out of scope for the simple loop.

## Hard gotchas

- **Input is wired in `onMount`, not a system.** `attachControls(canvas, ecs)` binds the canvas/keyboard listeners and returns a teardown App calls on unmount. Don't move this back to a `System` with a module-level `attached` flag — that goes stale across a remount (a fresh canvas keeps the old flag and never re-binds, so input silently dies).
- **The bake uses `forces`, not `invertRange`.** `invertRange` is the *exact* reflection inverse of the forward integrator (`θ_{i+1} = 2·m_i − θ_i`). It carries a leapfrog "computational mode": a marginally-stable ±(−1)^i tangent oscillation. On positions the integrator itself produced it cancels to zero (round-trip exact, so flat/ballistic/single-step tests pass), but on a varying-curvature curve the mode is excited and **F_n sawtooths sample-to-sample** — a real reported bug. (The current per-segment near-arcs *reduce* the excitation — the reflection is exact at constant curvature — but don't eliminate it, so the guard stays.) `forces` recovers θ as the curve's local tangent (bisector of adjacent chords) instead, which has no such mode. Don't switch `BakeSystem` to `invertRange` (the `track.test.ts` smoothness test guards this).
- **`forces` accumulates a *continuous* chord angle.** It unwraps the per-edge chord angle before taking the tangent bisector, so θ stays continuous across the ±π atan2 branch cut. The cart lerps θ for its orientation, so a raw-`atan2` θ would spin the cart a full turn between two samples when the heading crosses ±π (e.g. a leftward turn). A `bake.test.ts` test guards this.
- **Node headings (`Handle.theta`) are stored authored state — keep them out of the bake.** The bake's per-sample θ is a separate quantity, recovered from the sampled geometry (chord bisector). Don't (a) make the bake read `Handle.theta`; (b) reintroduce a `Track.theta0` entry angle — node 0's flat anchor *is* the entry; (c) switch `sampleChain` back to inferring tangents from neighbor positions each bake (Catmull-Rom) — that's the bidirectional cascade the stored-heading model kills, and it breaks two-segment locality. (See Model for how headings are maintained.)
- **`sampleChain` per-edge ds is the exact chord.** `dsArr[i] = |P_{i+1} − P_i|`. A near-coincident segment or `MAX_SAMPLES` truncation commits the prefix + orphans trailing nodes.
- **Forward clamps are non-differentiable.** A future optimizer's gradient is zero through `vSafe` / `sqrt` once tripped. The floor is tiny, so coasting past an infeasible region behaves like "cart paused at peak then continued" — negligible energy perturbation.
- **The constraint bezier's Y is absolute g, NOT theatre's value-delta normalization.** A handle `dy` is a g-delta from the keyframe value, so a *flat* segment (equal-g pins) with non-zero `dy` still bumps and can overshoot the band. Don't switch to value-delta-normalized handles (the theatre default) — it dead-flats equal-g segments and kills authored bumps. The overshoot survives the solve because the pin weight (1e4) dominates the band hinge (≈4).
- **Handle drag uses `valOf` (unclamped), never `valAt`.** `valAt` clamps to the band; a tangent must reach past it to author overshoot. Same trap as a band-clamped pin value — the overlay SVG is `overflow:visible` for exactly this.
- **Pin/handle snapshots must deep-copy the handle objects.** `Handle` is an object on the live `Pin`; a shallow `{...pin}` snapshot aliases `hl`/`hr`, so a live drag mutates the captured undo `prev` and silently breaks undo. `pinSnapshot`/`restorePin` clone both handles; `history`'s `commit` compares all four fields.
- **The single endpoint anchor heals *localized/balanced* reshapes, not wide sustained ones.** Applies to *any* deviation source now the anchor is always on — a pin, a bezier, or the band clamping the draft's own out-of-band forces. A point pin / doublet bezier (net ∫ΔF≈0) or a moderate band clamp heals to sub-decimetre drift; a *same-sign sustained* deviation across a span integrates a larger net heading change and the linearized single-shoot anchor under-heals (a −21g band clamp heals 58 m → ~2 m, ~28×; still a vast improvement over the un-anchored divergence, but not sub-cm). Not a bug in the anchor — force→geometry is fundamentally ill-conditioned (σ_pos ~N^1.56), so no force-space device (more anchors, multiple-shooting) drives the residual to cm. The real fix retires this whole path: the **position-space collocation re-architecture** (roadmap "kex2d Phase 3"; scratch.md "kex2d solver").
- **Quaternion DOF (when 3D lands).** Unit-norm constraint. Use the log-map (axis-angle delta) as the local update variable, matching `sim/curvature.rs::angular_delta_from`. Per-piece scope is unchanged in 3D.

## References

- **kexedit forward integrator** — `packages/core/src/sim/`. The 3D physics reference. `forward.ts` is the 2D forward direction (F_n → positions); `invertRange` is the reverse of the same equations. Core's node model: `nodes/force.rs` is F_n-driven, `nodes/geometric.rs` rate-driven, `sim/curvature.rs::from_frames` is quaternion-log curvature for the 3D port.
- **Houdini / Blender modifier stack** — analogue for lossy bake from parametric authoring into canonical dense state.
- **Witkin & Kass 1988, Spacetime Constraints** — parameter-space trajectory optimization with sparse user constraints; local support makes the optimizer's constraint influence decay rather than propagate globally.

## Verify

```bash
cd kex2d && bun check && bun test
cd kex2d && bun run capture   # UI screenshots → harness/shots/ (display-gated)
```

f64 mirror for tests: `tests/helpers/forward64.ts`. Independent physics check: `tests/oracles/rk4.ts` (time-parameterized RK4 — a different scheme + parameterization, catches sign/coupling bugs self-consistency tests can't). Physics is gated against the oracle, not self-consistency.

The ECS layer (`BakeSystem`, cart resampling) is covered device-free in `tests/track.test.ts` + `tests/cart.test.ts` (`BakeSystem` on a bare `State` via the scheduler). The `tests/setup.ts` enum-shim preload (`bunfig.toml`) lets them import the shallot barrel with no GPU device — see that file for why; the unit suite is canvas2D + device-free, no real-GPU leg.

`harness/` — Playwright **screenshot** harness (`bun run capture` → `harness/shots/`, gitignored) for UI review, **not a test gate** (no assertions). Self-contained sub-package outside the project `tsconfig`/`biome`, so `bun check` doesn't cover it; the orchestrator is Bun-only, just the staged `shot.pw.ts` imports Playwright. Drives the host's **real-GPU Chrome via the WSL→Windows bridge** — shallot's `run()` acquires a WebGPU device even though kex2d is canvas2D, so headless Linux won't do. Display-gated. Mirrors orrstead's harness + shallot's capture.
