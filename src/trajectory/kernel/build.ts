// Builds `lib.rs` to a shared-memory wasm-simd128 module and inlines it as base64 into
// `../kernel.wasm.ts`, which is committed so checks run without a Rust toolchain. Regenerate after any
// change here: bun src/trajectory/kernel/build.ts
//
// The flags are Shallot's shared physics build (`crates/physics/scripts/build-kernel.ts`), because the
// module boots on Shallot's pool, whose workers set `__stack_pointer` and call `__wasm_init_tls` into
// their own stack slice: +atomics needs core rebuilt on the dated nightly, and the stack size is the one
// the pool partitions. No wasm-opt pass: kexedit carries no binaryen.

import { spawnSync } from "node:child_process";
import { readFileSync, writeFileSync } from "node:fs";
import { resolve } from "node:path";

const NIGHTLY = "nightly-2026-09-10";
const STACK_SIZE = 3145728;
const MAX_MEMORY = 268435456;

const RUSTFLAGS = [
    "-C target-feature=+simd128,+atomics,+bulk-memory,+mutable-globals",
    "-C link-arg=--import-memory",
    "-C link-arg=--shared-memory",
    `-C link-arg=--max-memory=${MAX_MEMORY}`,
    `-C link-arg=-zstack-size=${STACK_SIZE}`,
    "-C link-arg=--export=__stack_pointer",
    "-C link-arg=--export=__tls_base",
    "-C link-arg=--export=__wasm_init_tls",
    "-C link-arg=--export=__heap_base",
    `--remap-path-prefix=${import.meta.dir}=`,
].join(" ");

const dir = import.meta.dir;
const result = spawnSync(
    "cargo",
    [`+${NIGHTLY}`, "build", "--release", "--target", "wasm32-unknown-unknown", "-Zbuild-std=core"],
    { cwd: dir, stdio: "inherit", env: { ...process.env, RUSTFLAGS } },
);
if (result.status !== 0) process.exit(result.status ?? 1);

const wasm = readFileSync(resolve(dir, "target/wasm32-unknown-unknown/release/kexedit_trajectory_kernel.wasm"));
const version = spawnSync("rustc", [`+${NIGHTLY}`, "--version"], { encoding: "utf8" }).stdout.trim();
writeFileSync(
    resolve(dir, "../kernel.wasm.ts"),
    `// Generated by src/trajectory/kernel/build.ts from src/trajectory/kernel/lib.rs — do not edit.
// Toolchain: ${version}. Shared-memory wasm-simd128 (${wasm.length} bytes), base64-inlined.
export const KERNEL_STACK_SIZE = ${STACK_SIZE};
export const KERNEL_MAX_MEMORY = ${MAX_MEMORY};
export const KERNEL_WASM_BASE64 =
    "${wasm.toString("base64")}";
`,
);
console.log(`[kernel] ${wasm.length} bytes`);
