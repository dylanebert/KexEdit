[package]
name = "kexedit-trajectory-kernel"
version = "0.0.0"
edition = "2021"
publish = false
description = "wasm-simd128 march and resample kernels over shared memory (build.ts)"

[lib]
crate-type = ["cdylib"]
path = "lib.rs"

[dependencies]
libm = "=0.2.16"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
